export const makeApiUrlAuthentication = (): string => {
  return '/login';
};

export const makeApiUrlClients = (): string => {
  return '/clients';
};

export const makeApiUrlEmployee = (): string => {
  return '/users';
};
