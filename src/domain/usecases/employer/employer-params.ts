export type EmployerParams = {
  page: string;
  size: string;
  userToken: string;
};

export type EmployersByIdParams = {
  id: string;
  userToken: string;
};

export type EmployerDeleteParams = {
  id: string;
  userToken: string;
};
