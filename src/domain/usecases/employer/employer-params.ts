export type EmployeeParams = {
  page: string;
  size: string;
  userToken: string;
};

export type EmployeeByIdParams = {
  id: string;
  userToken: string;
};

export type EmployeeDeleteParams = {
  id: string;
  userToken: string;
};
